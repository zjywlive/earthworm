{
  "name": "earthworm-local",
  "version": "1.0.0",
  "description": "Earthworm 英语学习应用 - 纯本地版",
  "private": true,
  "scripts": {
    "dev": "pnpm -F client dev",
    "build": "pnpm -F client build",
    "preview": "pnpm -F client preview"
  },
  "engines": {
    "node": ">=20.12.2"
  }
}
